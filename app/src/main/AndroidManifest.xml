<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.holmeslei.rxjavademo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.rxjava1.CreatActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.rxjava1.TransformActivity" />
        <activity android:name=".ui.rxjava1.FilterActivity" />
        <activity android:name=".ui.rxjava1.ComposeActivity" />
        <activity android:name=".ui.rxjava1.PracticeActivity" />
        <activity android:name=".ui.rxjava2.BackPressureActivity"></activity>
    </application>

</manifest>